
class VulkanInstance;
class RenderAssets;

class ImageRenderer {
public:
  ImageRenderer() = default;
  ~ImageRenderer() = default;
  void init(VulkanInstance* instance, RenderAssets* assets);
private:

};
